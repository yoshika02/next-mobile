I've removed the sharp, dark window bar shadows that were causing the issue.

### Changes:
1.  **Updated `app/vidclue/page.tsx`:** Removed the `.shadow-window-bars` overlay completely.

The scene is now much cleaner, retaining only the subtle texture from the room overlay without those heavy, distracting dark bands across the device.

Check it out with `npm run dev`!