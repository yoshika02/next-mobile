I've reverted the background changes as requested.

### Changes:
1.  **Main Background:** Restored `bg-room.png` as the main site background image.
2.  **Removed Light Effects:** Removed the "window blinds" / light shadow overlay.
3.  **Cleaned Up Screens:** Removed the room reflection overlays from inside the device screens, as the device is now back "in" the room.

The site is now back to the state where the device is floating in the room environment, without the additional light overlays.

Check it out with `npm run dev`!