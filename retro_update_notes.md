I've significantly enhanced the retro effect to match your "blinds shadow" and textured look description.

### Changes:
1.  **Global "Blinds" Shadow:** Added a full-screen, repeating linear gradient shadow (`.shadow-blinds`) that covers both the background and the device. This creates the window blinds effect casting shadows across the entire scene.
2.  **Enhanced Grain Texture:** Updated the `.bg-grain` SVG filter to be more "granular" (lower frequency) and increased its opacity on the device case (`opacity-60`, `mix-blend-soft-light`) to give the red casing that rough, speckled plastic texture.
3.  **Light Environment:** Changed the main background color from dark `bg-stone-800` to light `bg-gray-100` to match the reference image where the device is sitting on a desk or against a wall in daylight.
4.  **Right Screen Update:** Added the "Scroll to see more" floating pill at the bottom of the right screen, as seen in the reference.

The result should now have:
-   A light, airy environment.
-   Strong, realistic diagonal shadows (blinds) across everything.
-   A very textured, tactile red casing.
-   The "retro sepia" screen tint remains.

Check it out now! The "plainness" should be gone.